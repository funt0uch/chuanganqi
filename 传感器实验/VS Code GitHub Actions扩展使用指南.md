# VS Code GitHub Actions扩展使用指南

## 扩展功能

这个扩展可以让你在VS Code中：
- ✅ 直接查看GitHub Actions的运行状态
- ✅ 查看构建日志和错误信息
- ✅ 管理workflow文件
- ✅ 触发workflow运行

## 使用步骤

### 第一步：连接GitHub账号

1. **打开命令面板**：
   - 按 `Ctrl+Shift+P` (Windows) 或 `Cmd+Shift+P` (Mac)
   - 输入：`GitHub: Sign in`

2. **授权VS Code**：
   - 会打开浏览器，登录GitHub账号
   - 授权VS Code访问你的仓库

### 第二步：打开GitHub仓库

#### 方法1：克隆仓库到本地
```bash
# 在VS Code终端中
git clone https://github.com/你的用户名/仓库名.git
cd 仓库名
code .
```

#### 方法2：打开已有文件夹
1. File > Open Folder
2. 选择你的项目文件夹

### 第三步：查看Actions状态

1. **打开Actions面板**：
   - 点击左侧边栏的GitHub图标（或按 `Ctrl+Shift+G`）
   - 选择 "Actions" 标签

2. **查看workflow运行**：
   - 会显示所有workflow的运行状态
   - 绿色✅ = 成功
   - 红色❌ = 失败
   - 黄色🟡 = 进行中

### 第四步：查看错误日志

1. **点击失败的workflow**：
   - 在Actions面板中点击红色❌的workflow

2. **查看详细日志**：
   - 展开失败的步骤
   - 查看错误输出
   - 错误信息会高亮显示

3. **复制错误信息**：
   - 选中错误文本
   - 复制后可以分析问题

## 常用操作

### 查看workflow文件

1. **打开workflow文件**：
   - 在文件浏览器中找到 `.github/workflows/build.yml`
   - 双击打开编辑

2. **语法高亮**：
   - VS Code会自动识别YAML文件
   - 提供语法高亮和错误检查

### 编辑workflow

1. **直接编辑**：
   - 在VS Code中编辑workflow文件
   - 保存后会自动提示提交

2. **提交更改**：
   - 点击源代码管理图标（左侧）
   - 输入提交信息
   - 点击提交并推送

### 触发workflow

1. **手动触发**：
   - 在Actions面板中
   - 右键点击workflow
   - 选择 "Run Workflow"

## 调试构建失败

### 查看完整日志

1. **在Actions面板中**：
   - 点击失败的workflow运行
   - 展开所有步骤
   - 查看每个步骤的输出

2. **常见错误位置**：
   - "Install system dependencies" - 系统依赖安装失败
   - "Install Python dependencies" - Python包安装失败
   - "Build APK with Buildozer" - APK构建失败（最常见）

### 分析错误

1. **复制错误信息**：
   - 选中错误文本
   - `Ctrl+C` 复制

2. **常见错误类型**：

   **错误1：找不到文件**
   ```
   FileNotFoundError: buildozer.spec
   ```
   **解决**：确保所有文件都已提交到GitHub

   **错误2：模块导入失败**
   ```
   ModuleNotFoundError: No module named 'sensors'
   ```
   **解决**：检查文件结构，确保sensors文件夹存在

   **错误3：Buildozer配置错误**
   ```
   buildozer.spec error
   ```
   **解决**：检查buildozer.spec文件格式

## 快速修复流程

### 1. 查看错误
- 在VS Code Actions面板查看失败原因

### 2. 修复问题
- 根据错误信息修改代码或配置

### 3. 提交更改
```bash
git add .
git commit -m "Fix build error"
git push
```

### 4. 等待重新构建
- 推送后会自动触发新的构建
- 在Actions面板查看进度

## 实用技巧

### 技巧1：实时查看构建进度
- Actions面板会实时更新
- 可以看到构建的每个步骤

### 技巧2：比较不同运行
- 可以对比成功和失败的运行
- 找出问题所在

### 技巧3：查看历史记录
- 查看所有历史构建
- 分析失败模式

## 下一步

1. ✅ 连接GitHub账号
2. ✅ 打开项目文件夹
3. ✅ 查看Actions面板
4. ✅ 分析失败原因
5. ✅ 修复并重新构建

## 需要帮助？

如果遇到问题：
1. 查看VS Code输出面板（View > Output）
2. 选择"GitHub"查看扩展日志
3. 检查GitHub连接状态

